<h2><%= @cat.name %></h2>
<p><%= @cat.details %></p>
<% if policy(@cat).edit? %>
  <%= link_to "Edit this cat", edit_cat_path(@cat) %> |
<% end %>
  <%= link_to "heart", cat_matches_path(@cat) %>
<%= button_to "Destroy this cat", @cat, method: :delete if policy(@cat).destroy? %>
  <%= link_to "back", cats_path %>
